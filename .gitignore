# --- Secrets / credentials ---------------------------------------------------
# DPAPI-encrypted, but still machine/user-bound and not meant to be shared.
creds.xml
# Legacy plaintext config (pre-migration). Never commit.
config.txt

# --- Runtime artifacts -------------------------------------------------------
login.log
*.log
PowerShell_transcript.*
# Captive-portal HTML dump regenerated on every gated run.
portal-debug.html

# --- Tooling / editor --------------------------------------------------------
.claude/
.vscode/
.idea/

# --- Windows cruft -----------------------------------------------------------
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/
*.lnk.bak

# --- Temporary files ---------------------------------------------------------
*.tmp
*.temp
*.bak
*.swp
*~

# --- Release artifacts (built locally, attached to GitHub releases) ---------
sliit-wifi-v*.zip
.release-notes-v*.md
